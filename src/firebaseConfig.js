// Paste your Firebase web app config here (Firebase console → Project settings
// → Your apps → Web app → SDK setup and configuration → Config).
// This config is safe to commit: it identifies the project, it is not a secret.
// Access control lives in firestore.rules, not here.
//
// Spark (free) plan only. Do NOT enable Blaze or add billing — nothing in this
// app needs it.
export const firebaseConfig = {
  apiKey: 'PASTE_API_KEY',
  authDomain: 'PASTE_PROJECT_ID.firebaseapp.com',
  projectId: 'PASTE_PROJECT_ID',
  storageBucket: 'PASTE_PROJECT_ID.appspot.com',
  messagingSenderId: 'PASTE_SENDER_ID',
  appId: 'PASTE_APP_ID',
}
